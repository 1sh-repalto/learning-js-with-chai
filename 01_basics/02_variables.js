let a = 1;
let b = 2;
let c = 3;

// console.table prints values of an array in tabular from
console.table([a, b, c]);

//prefer not to use var because of issue in block scope and functional scope, instead use let